@model IEnumerable<ResultSellingProductDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

<h3>En Çok Satan Ürünler</h3>

<table class="table table-bordered table-hover">
    <tr>
        <th>#</th>
        <th>Görsel</th>
        <th>Ürün Adı</th>
        <th>Fiyat</th>
        <th>İşlemler</th>
    </tr>
    @foreach (var item in Model)
    {
        count++;
        <tr>
            <th>@count</th>
            <th><img src="@item.ImageUrl" style="height:75px; width:75px;" /></th>
            <td>@item.Name</td>
            <td>@item.Price ₺</td>
            <td>
                <a href="/SellingProduct/DeleteSellingProduct/@item.SellingProductId" class="btn btn-danger">Sil</a>
                <a href="/SellingProduct/UpdateSellingProduct/@item.SellingProductId" class="btn btn-info">Güncelle</a>
            </td>
        </tr>
    }
</table>

<a href="/SellingProduct/CreateSellingProduct" class="btn btn-info">Yeni En Çok Satan Ürün Oluştur</a>